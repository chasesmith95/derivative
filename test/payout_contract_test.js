var assert = require('assert');

describe('Payout Contract', function() {
    it('should be created', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('normalization of change', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('correct weight calculation', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
});
