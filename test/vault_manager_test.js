var assert = require('assert');
describe('Vault Manager', function() {
    describe('Managing External Contracts', function() {
  describe('Managing Oracles', function() {
    it('should be able to be created', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('should be able to be added', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('should be removed', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('shoud be retrieved', function(){
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });

  describe('Managing Payout Contracts', function() {
    it('should be able to be added', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('should be removed', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
    it('shoud be retrieved', function(){
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });

  describe('Managing Vaults', function() {
    it('should be able to be created', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});

  describe('Payments', function() {
    describe('Vault Construction Fees', function() {
      it('Activation', function() {
        assert.equal([1, 2, 3].indexOf(4), -1);
      });
    });

    describe('Collecting Creation Fees', function() {
      it('Activation', function() {
        assert.equal([1, 2, 3].indexOf(4), -1);
      });
    });

    describe('Receiving Collateral', function() {
      it('Activation', function() {
        assert.equal([1, 2, 3].indexOf(4), -1);
      });
    });

    describe('Sending Collateral', function() {
      it('Activation', function() {
        assert.equal([1, 2, 3].indexOf(4), -1);
      });
    });
  });
});
