var assert = require('assert');

describe('Oracle', function() {
    it('should be able to be constructed', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });

    it('should do queries', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });

    it('should do past queries', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
});
